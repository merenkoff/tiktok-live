# Optional override. Cashier Vite config already defaults to https://the-live.shop
# (clone + npm run tauri:dev / tauri:build). Web `npm run dev` still uses the
# local proxy unless you set this. Empty = same origin.
# VITE_API_BASE=https://the-live.shop
